<Window
    x:Class="SnabBashka.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SnabBashka"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Main"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <local:ViewModelLocator x:Key="VMLocator" />
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <Binding Path="MainViewModel" Source="{StaticResource VMLocator}" />
    </Window.DataContext>
    <Grid>
        <Frame Content="{Binding CurrentPage}" NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
